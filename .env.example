# Backend Application Environment Variables
NODE_ENV=development
PORT=3000

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=criterium_edu

# JWT Configuration
JWT_SECRET=your_very_secret_jwt_key_here_please_change_me
JWT_EXPIRATION_TIME=1d

# OpenAI API Configuration
OPENAI_API_KEY=*****

# Logging Configuration
LOG_LEVEL=info

# Frontend Environment Variables (example, manage as needed)
VITE_API_URL=http://localhost:3000/api

# Container Registry Settings (from your original .env.example, keep if needed for Docker)
REGISTRY=ghcr.io
GITHUB_REPOSITORY_OWNER=mdportnov
BACKEND_IMAGE=criterium-edu-backend
FRONTEND_IMAGE=criterium-edu-frontend
IMAGE_TAG=latest
